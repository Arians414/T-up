import { LogModalBase, type LogModalProps } from './LogModalBase';

export const MorningErectionLogModal = (props: LogModalProps) => {
  return <LogModalBase metricId="morningErection" {...props} />;
};
